---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="FM27 Hub | Startseite">
  
  <!-- Weltkarte / Karriere-Stationen -->
  <div class="bg-zinc-900 border border-zinc-800 rounded-xl p-6 text-center my-6">
    <h2 class="text-2xl font-bold text-[#e5c158] mb-1">Meine Karriere-Stationen</h2>
    <p class="text-zinc-400 text-sm mb-4">Interaktive Übersicht aller Vereinsstationen im FM27</p>

    <!-- Karten-Container -->
    <div id="map" class="w-full h-[400px] rounded-lg overflow-hidden border border-zinc-800 relative z-0"></div>
  </div>

  <!-- Leaflet CSS & JS über CDN -->
  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
  <script is:inline src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>

  <script is:inline>
    document.addEventListener('DOMContentLoaded', () => {
      // Karte initialisieren (Startposition Europa, Zoomstufe 4)
      const map = L.map('map').setView([50.8322, 12.9253], 4);

      // Dunkle Kartenebene einbinden
      L.tileLayer('https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png', {
        attribution: '&copy; OpenStreetMap &copy; CARTO',
        subdomains: 'abcd',
        maxZoom: 19
      }).addTo(map);


      //
      // Liste deiner Trainer-Stationen (Koordinaten: Latitude, Longitude)
      //

      const stationen = [
        { name: "Chemnitzer FC", lat: 50.8322, lng: 12.9253, status: "Aktueller Verein", saison: "2026/27" },
        // Hier kannst du zukünftig weitere Vereine anhängen:
        // { name: "Real Madrid", lat: 40.4531, lng: -3.6883, status: "Ehemalige Station", saison: "2028/29" },
      ];

      // Marker für jede Station hinzufügen
      stationen.forEach(st => {
        L.marker([st.lat, st.lng])
          .addTo(map)
          .bindPopup(`
            <div style="color: #111; font-family: sans-serif;">
              <strong style="font-size: 1.1em;">${st.name}</strong><br>
              <span style="color: #555;">${st.status}</span><br>
              <small><b>Saison:</b> ${st.saison}</small>
            </div>
          `);
      });
    });
  </script>

  <h2 class="section-title">Die Reise beginnt</h2>

  <div class="card-grid">
    <div class="card">
      <h3>📖 Neue Trainerstory</h3>
      <p>kommt noch</p>
    </div>
    <div class="card">
      <h3>Erfolge</h3>
      <p>kommt noch</p>
    </div>
    <div class="card">
      <h3>🎨 alles was ich zum download habe </h3>
      <p>Medienpaket, taktiken, Real-Name-Fixes (`.lnc`) training usw.</p>
    </div>
  </div>

  

</BaseLayout>